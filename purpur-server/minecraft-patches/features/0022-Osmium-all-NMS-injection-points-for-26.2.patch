From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Djatlasv <djatlasv@users.noreply.github.com>
Date: Sun, 24 Aug 2026 11:20:00 -0600
Subject: [PATCH] Osmium: all NMS injection points for 26.2

diff --git a/net/minecraft/commands/Commands.java b/net/minecraft/commands/Commands.java
index 38bd5b1..d8b79a9 100644
--- a/net/minecraft/commands/Commands.java
+++ b/net/minecraft/commands/Commands.java
@@ -264,6 +264,11 @@ public class Commands {
         WaypointCommand.register(this.dispatcher, context);
         WeatherCommand.register(this.dispatcher);
         WorldBorderCommand.register(this.dispatcher);
+        org.osmium.OsmiumRtp.registerCommand(this.dispatcher); // Osmium - /rtp command (always register; enabled check is at execution time)
+        org.osmium.OsmiumTeam.registerCommand(this.dispatcher); // Osmium - /team command (always register; enabled check is at execution time)
+        org.osmium.OsmiumTpa.registerCommand(this.dispatcher); // Osmium - /tpa /tpaccept /tpdeny
+        org.osmium.OsmiumHomes.registerCommand(this.dispatcher); // Osmium - /sethome /home /homes /spawn
+        org.osmium.OsmiumRecipes.registerCommand(this.dispatcher); // Osmium - /customrecipes GUI + reload
         if (JvmProfiler.INSTANCE.isAvailable()) {
             JfrCommand.register(this.dispatcher);
         }
diff --git a/net/minecraft/network/protocol/game/ClientboundLevelChunkPacketData.java b/net/minecraft/network/protocol/game/ClientboundLevelChunkPacketData.java
index a52f93d..4061384 100644
--- a/net/minecraft/network/protocol/game/ClientboundLevelChunkPacketData.java
+++ b/net/minecraft/network/protocol/game/ClientboundLevelChunkPacketData.java
@@ -71,6 +71,17 @@ public class ClientboundLevelChunkPacketData {
                 }
             }
             // Paper end - Handle oversized block entities in chunks
+            // Osmium start - hide occluded block entities
+            // Player comes from the packet info, NOT the CURRENT_PLAYER
+            // ThreadLocal: getChunkPacketInfo() clears that before this
+            // constructor runs, so the ThreadLocal is always null here.
+            if (chunkPacketInfo instanceof org.osmium.anticheat.OsmiumChunkPacketInfo osmiumInfo
+                    && org.osmium.anticheat.OsmiumOcclusion.shouldHideBlockEntity(
+                        (net.minecraft.server.level.ServerLevel) levelChunk.getLevel(),
+                        osmiumInfo.getPlayer(), entry.getKey())) {
+                continue;
+            }
+            // Osmium end - hide occluded block entities
             this.blockEntitiesData.add(ClientboundLevelChunkPacketData.BlockEntityInfo.create(entry.getValue()));
         }
     }
diff --git a/net/minecraft/server/MinecraftServer.java b/net/minecraft/server/MinecraftServer.java
index d9402a7..94a164d 100644
--- a/net/minecraft/server/MinecraftServer.java
+++ b/net/minecraft/server/MinecraftServer.java
@@ -258,6 +258,7 @@ public abstract class MinecraftServer extends ReentrantBlockableEventLoop<TickTa
     protected final Services services;
     private final NotificationManager notificationManager;
     private final ServerActivityMonitor serverActivityMonitor;
+    private volatile org.osmium.OsmiumBackups.MinecraftServerLike osmiumBackupLike; // Osmium - backups
     private long lastServerStatus;
     private final Thread serverThread;
     private long lastTickNanos = Util.getNanos();
@@ -650,6 +651,7 @@ public abstract class MinecraftServer extends ReentrantBlockableEventLoop<TickTa
         }
         // Paper end - Configurable player collision; Handle collideRule team for player collision toggle
         this.server.enablePlugins(org.bukkit.plugin.PluginLoadOrder.POSTWORLD);
+        if (org.osmium.OsmiumConfig.grimEnabled) { org.osmium.anticheat.grim.OsmiumGrimLoader.start(); } // Osmium - GrimAC
         this.server.spark.registerCommandBeforePlugins(this.server); // Paper - spark
         this.server.spark.enableAfterPlugins(this.server); // Paper - spark
         io.papermc.paper.command.brigadier.PaperCommands.INSTANCE.setValid(); // Paper - reset invalid state for event fire below
@@ -1299,6 +1301,7 @@ public abstract class MinecraftServer extends ReentrantBlockableEventLoop<TickTa
             // tasks are default scheduled at -1 + delay, and first tick will tick at 1
             final long actualDoneTimeMs = System.currentTimeMillis() - org.bukkit.craftbukkit.Main.BOOT_TIME.toEpochMilli(); // Paper - Improve startup message
             LOGGER.info("Done ({})! For help, type \"help\"", String.format(java.util.Locale.ROOT, "%.3fs", actualDoneTimeMs / 1000.00D)); // Paper - Improve startup message
+            org.osmium.OsmiumUpdateChecker.checkAsync(); // Osmium - update check
             org.spigotmc.WatchdogThread.tick();
             // Paper end
             org.spigotmc.WatchdogThread.hasStarted = true; // Paper
@@ -1896,6 +1899,29 @@ public abstract class MinecraftServer extends ReentrantBlockableEventLoop<TickTa
 
         profiler.pop();
         this.serverActivityMonitor.tick();
+
+    // Osmium start - tick processing
+        org.osmium.anticheat.OsmiumBrandEnforcement.tick(this.tickCount, this); // Osmium - brand enforcement
+        org.osmium.anticheat.OsmiumOcclusion.tick(this); // Osmium - raytrace visibility + entity occlusion drivers
+        org.osmium.OsmiumRtp.tick(this);
+        if (org.osmium.OsmiumConfig.scoreboardEnabled && this.tickCount % org.osmium.OsmiumConfig.scoreboardUpdateTicks == 0) { org.osmium.OsmiumScoreboard.tick(this); }
+        if (org.osmium.OsmiumConfig.teamEnabled) { org.osmium.OsmiumTeam.tick(this); } // Osmium - team invite expiry
+        if (org.osmium.OsmiumConfig.tpaEnabled) { org.osmium.OsmiumTpa.tick(this); } // Osmium - tpa expiry/countdowns
+        if (org.osmium.OsmiumConfig.homesEnabled) { org.osmium.OsmiumHomes.tick(this); } // Osmium - home countdowns
+        if (org.osmium.OsmiumConfig.spawnDimensionEnabled) { org.osmium.OsmiumSpawnDim.tick(this); } // Osmium - spawn dimension
+        org.osmium.OsmiumCombat.tick(this); // Osmium - combat tag action bar / expiry
+        // Osmium start - auto backups
+        if (org.osmium.OsmiumConfig.backupsEnabled) {
+            if (this.osmiumBackupLike == null) {
+                this.osmiumBackupLike = new org.osmium.OsmiumBackups.MinecraftServerLike() {
+                    @Override public long currentTick() { return MinecraftServer.this.tickCount; }
+                    @Override public void saveAllChunks() { MinecraftServer.this.saveEverything(false, true, true); }
+                };
+            }
+            org.osmium.OsmiumBackups.tick(this.osmiumBackupLike);
+        }
+        // Osmium end - auto backups
+        // Osmium end
     }
 
     // Paper start - per world respawn data
diff --git a/net/minecraft/server/commands/BanPlayerCommands.java b/net/minecraft/server/commands/BanPlayerCommands.java
index c87bc86..3970f74 100644
--- a/net/minecraft/server/commands/BanPlayerCommands.java
+++ b/net/minecraft/server/commands/BanPlayerCommands.java
@@ -45,6 +45,13 @@ public class BanPlayerCommands {
                 list.add(entry);
                 count++;
                 source.sendSuccess(() -> Component.translatable("commands.ban.success", Component.literal(player.name()), entry.getReasonMessage()), true);
+                // Osmium start - discord webhook for bans
+                org.osmium.anticheat.OsmiumDiscordWebhook.sendBan(
+                    player.name(), player.id(),
+                    reason == null ? null : reason.getString(),
+                    source.getTextName()
+                );
+                // Osmium end - discord webhook for bans
                 ServerPlayer online = source.getServer().getPlayerList().getPlayer(player.id());
                 if (online != null) {
                     online.connection.disconnect(Component.translatable("multiplayer.disconnect.banned"), org.bukkit.event.player.PlayerKickEvent.Cause.BANNED); // Paper - kick event cause
diff --git a/net/minecraft/server/dedicated/DedicatedServer.java b/net/minecraft/server/dedicated/DedicatedServer.java
index 9b19888..0821742 100644
--- a/net/minecraft/server/dedicated/DedicatedServer.java
+++ b/net/minecraft/server/dedicated/DedicatedServer.java
@@ -244,10 +244,20 @@ public class DedicatedServer extends MinecraftServer implements ServerInterface
         }
         org.purpurmc.purpur.PurpurConfig.registerCommands();
         // Purpur end - Migrate Setting to reintroduce end void rings
+        // Osmium start
+        org.osmium.OsmiumConfig.init((java.io.File) options.valueOf("osmium-settings"));
+        org.osmium.OsmiumRtp.initPermission();
+        org.osmium.OsmiumTeam.initPermission();
+        org.osmium.anticheat.OsmiumAltTracker.init(new java.io.File("."));
+        org.osmium.anticheat.OsmiumChatFilter.init(new java.io.File("."));
+        org.osmium.OsmiumTeam.init(new java.io.File("."));
+        org.osmium.OsmiumHomes.init(new java.io.File("."));
+        // Osmium end
         // Paper start - initialize global and world-defaults configuration
         this.paperConfigurations.initializeGlobalConfiguration(this.registryAccess());
         this.paperConfigurations.initializeWorldDefaultsConfiguration(this.registryAccess());
         // Paper end - initialize global and world-defaults configuration
+        if (org.osmium.OsmiumConfig.grimEnabled) { org.osmium.anticheat.grim.OsmiumGrimLoader.init(); } // Osmium - GrimAC (after Paper config init)
         this.server.spark.enableEarlyIfRequested(); // Paper - spark
         // Paper start - fix converting txt to json file; convert old users earlier after PlayerList creation but before file load/save
         if (this.convertOldUsers()) {
@@ -415,6 +425,9 @@ public class DedicatedServer extends MinecraftServer implements ServerInterface
 
         this.saveEverything(false, true, true);
         this.notificationManager().serverStarted();
+        org.osmium.anticheat.OsmiumDiscordWebhook.sendServerStart(); // Osmium - discord webhook
+        org.osmium.discord.OsmiumDiscordBot.start(new java.io.File(".")); // Osmium - discord bot (after server fully ready)
+        org.osmium.OsmiumRecipes.loadAll(this); // Osmium - custom recipe loading (after registries ready)
         return true;
     }
 
@@ -951,6 +964,11 @@ public class DedicatedServer extends MinecraftServer implements ServerInterface
 
     @Override
     protected void stopServer() {
+        org.osmium.discord.OsmiumDiscordBot.stop(); // Osmium - disconnect discord bot first
+        org.osmium.anticheat.OsmiumAltTracker.flush(); // Osmium - persist alt tracker data before shutdown
+        org.osmium.OsmiumTeam.flush(); // Osmium - persist teams before shutdown
+        org.osmium.anticheat.OsmiumDiscordWebhook.sendServerStop(); // Osmium - discord webhook (sync, before shutdown)
+        if (org.osmium.OsmiumConfig.grimEnabled) { org.osmium.anticheat.grim.OsmiumGrimLoader.stop(); } // Osmium - GrimAC
         this.notificationManager().serverShuttingDown();
         super.stopServer();
         //Util.shutdownExecutors(); // Paper - Improved watchdog support; moved into super
diff --git a/net/minecraft/server/level/ChunkHolder.java b/net/minecraft/server/level/ChunkHolder.java
index cd5a325..5be7b9c 100644
--- a/net/minecraft/server/level/ChunkHolder.java
+++ b/net/minecraft/server/level/ChunkHolder.java
@@ -278,10 +278,28 @@ public class ChunkHolder extends GenerationChunkHolder implements ca.spottedleaf
             if (!this.skyChangedLightSectionFilter.isEmpty() || !this.blockChangedLightSectionFilter.isEmpty()) {
                 List<ServerPlayer> borderPlayers = this.moonrise$getPlayers(true); // Paper - rewrite chunk system
                 if (!borderPlayers.isEmpty()) {
-                    ClientboundLightUpdatePacket lightPacket = new ClientboundLightUpdatePacket(
-                        chunk.getPos(), this.lightEngine, this.skyChangedLightSectionFilter, this.blockChangedLightSectionFilter
-                    );
-                    this.broadcast(borderPlayers, lightPacket);
+                    // Osmium start - filter light updates for hidden sections (defeats Light Finder)
+                    BitSet skyFilter = this.skyChangedLightSectionFilter;
+                    BitSet blockFilter = this.blockChangedLightSectionFilter;
+                    if (org.osmium.OsmiumConfig.chunkHidingEnabled && org.osmium.OsmiumConfig.chunkHidingHideLight) {
+                        skyFilter = (BitSet) skyFilter.clone();
+                        blockFilter = (BitSet) blockFilter.clone();
+                        int minLightSection = this.levelHeightAccessor.getMinSectionY() - 1;
+                        int hideBelowSection = org.osmium.OsmiumConfig.chunkHidingYThreshold >> 4;
+                        for (int i = skyFilter.nextSetBit(0); i >= 0; i = skyFilter.nextSetBit(i + 1)) {
+                            if (minLightSection + i < hideBelowSection) skyFilter.clear(i);
+                        }
+                        for (int i = blockFilter.nextSetBit(0); i >= 0; i = blockFilter.nextSetBit(i + 1)) {
+                            if (minLightSection + i < hideBelowSection) blockFilter.clear(i);
+                        }
+                    }
+                    if (!skyFilter.isEmpty() || !blockFilter.isEmpty()) {
+                        ClientboundLightUpdatePacket lightPacket = new ClientboundLightUpdatePacket(
+                            chunk.getPos(), this.lightEngine, skyFilter, blockFilter
+                        );
+                        this.broadcast(borderPlayers, lightPacket);
+                    }
+                    // Osmium end
                 }
 
                 this.skyChangedLightSectionFilter.clear();
diff --git a/net/minecraft/server/level/ChunkMap.java b/net/minecraft/server/level/ChunkMap.java
index 7e93614..7ab0e30 100644
--- a/net/minecraft/server/level/ChunkMap.java
+++ b/net/minecraft/server/level/ChunkMap.java
@@ -1406,6 +1406,24 @@ public class ChunkMap extends SimpleRegionStorage implements ChunkHolder.PlayerP
                     visibleToPlayer = false;
                 }
                 // CraftBukkit end
+                // Osmium start - hide entities below chunk hiding threshold
+                if (visibleToPlayer && org.osmium.OsmiumConfig.chunkHidingEnabled && org.osmium.OsmiumConfig.chunkHidingHideEntities
+                        && !(this.entity instanceof ServerPlayer) // never hide other players
+                        && this.entity.getY() < org.osmium.OsmiumConfig.chunkHidingYThreshold) {
+                    double proxSq = (double) org.osmium.OsmiumConfig.chunkHidingProximityRadius * org.osmium.OsmiumConfig.chunkHidingProximityRadius;
+                    double dx = player.getX() - this.entity.getX();
+                    double dy = player.getY() - this.entity.getY();
+                    double dz = player.getZ() - this.entity.getZ();
+                    if (dx * dx + dy * dy + dz * dz > proxSq) {
+                        visibleToPlayer = false;
+                    }
+                }
+                // Osmium end
+                // Osmium start - raycasted entity occlusion
+                if (visibleToPlayer && org.osmium.anticheat.OsmiumOcclusion.isEntityOccluded(player, this.entity)) {
+                    visibleToPlayer = false;
+                }
+                // Osmium end - raycasted entity occlusion
                 if (visibleToPlayer) {
                     if (this.seenBy.add(player.connection)) {
                         // Paper start - entity tracking events
diff --git a/net/minecraft/server/level/ServerPlayer.java b/net/minecraft/server/level/ServerPlayer.java
index d1dce39..7cfe335 100644
--- a/net/minecraft/server/level/ServerPlayer.java
+++ b/net/minecraft/server/level/ServerPlayer.java
@@ -732,6 +732,7 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
         }
         // CraftBukkit end
         this.connection.tickClientLoadTimeout();
+        org.osmium.anticheat.OsmiumYLevelTracker.onPlayerTick(this); // Osmium - y-level hiding chunk resend
         this.gameMode.tick();
         this.wardenSpawnTracker.tick();
         if (this.invulnerableTime > 0) {
@@ -1150,6 +1151,9 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
     // Paper end - Expand PlayerDeathEvent API
     @Override
     public void die(final DamageSource source) {
+        org.osmium.discord.OsmiumDiscordBot.onPlayerEvent("\u2620 " + this.getPlainTextName() + " " +
+                this.getCombatTracker().getDeathMessage().getString()); // Osmium - discord bridge
+        org.osmium.OsmiumCombat.clear(this.getUUID()); // Osmium - combat tag cleared on death
         // this.gameEvent(GameEvent.ENTITY_DIE); // Paper - move below event cancellation check
         boolean showDeathMessage = this.level().getGameRules().get(GameRules.SHOW_DEATH_MESSAGES);
         // CraftBukkit start - fire PlayerDeathEvent
@@ -1325,6 +1329,14 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
             }
         }
         // Purpur end - Add boat fall damage config
+        // Osmium start - team friendly fire + combat tag
+        if (source.getEntity() instanceof ServerPlayer ffAttacker) {
+            if (org.osmium.OsmiumTeam.isFriendlyFireBlocked(this, ffAttacker)) {
+                return false;
+            }
+            org.osmium.OsmiumCombat.tag(this, ffAttacker);
+        }
+        // Osmium end - team friendly fire + combat tag
         Entity entity = source.getEntity();
         if (!( // Paper - split the if statement. If below statement is false, hurtServer would not have been evaluated. Return false.
             !(entity instanceof Player player && !this.canHarmPlayer(player))
@@ -1360,6 +1372,14 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
         final org.bukkit.event.player.PlayerRespawnEvent.RespawnReason respawnReason
     ) {
         RespawnResult result = this.findRespawnPositionAndUseSpawnBlock0(consumeSpawnBlock, postTeleportTransition, respawnReason);
+        // Osmium start - spawn dimension default death respawn
+        if (result != null && !result.isBedSpawn() && !result.isAnchorSpawn()
+                && respawnReason == org.bukkit.event.player.PlayerRespawnEvent.RespawnReason.DEATH
+                && org.osmium.OsmiumConfig.spawnDimensionEnabled) {
+            var osmiumTransition = org.osmium.OsmiumSpawnDim.respawnTransition(this, postTeleportTransition);
+            if (osmiumTransition != null) return osmiumTransition;
+        }
+        // Osmium end - spawn dimension default death respawn
         return result == null ? null : result.transition();
     }
 
diff --git a/net/minecraft/server/level/ThreadedLevelLightEngine.java b/net/minecraft/server/level/ThreadedLevelLightEngine.java
index 948a8a2..0d2ed97 100644
--- a/net/minecraft/server/level/ThreadedLevelLightEngine.java
+++ b/net/minecraft/server/level/ThreadedLevelLightEngine.java
@@ -130,9 +130,26 @@ public class ThreadedLevelLightEngine extends LevelLightEngine implements AutoCl
                             return;
                         }
 
+                        // Osmium start - filter light updates for hidden sections
+                        java.util.BitSet skyFilter = null;
+                        java.util.BitSet blockFilter = null;
+                        if (org.osmium.OsmiumConfig.chunkHidingEnabled && org.osmium.OsmiumConfig.chunkHidingHideLight) {
+                            final int minLightSection = ThreadedLevelLightEngine.this.getMinLightSection();
+                            final int lightSectionCount = ThreadedLevelLightEngine.this.getLightSectionCount();
+                            final int hideBelowSection = org.osmium.OsmiumConfig.chunkHidingYThreshold >> 4;
+                            skyFilter = new java.util.BitSet(lightSectionCount);
+                            blockFilter = new java.util.BitSet(lightSectionCount);
+                            for (int li = 0; li < lightSectionCount; li++) {
+                                if (minLightSection + li >= hideBelowSection) {
+                                    skyFilter.set(li);
+                                    blockFilter.set(li);
+                                }
+                            }
+                        }
+                        // Osmium end
                         final net.minecraft.network.protocol.Packet<?> relightPacket = new net.minecraft.network.protocol.game.ClientboundLightUpdatePacket(
                             pos, (ThreadedLevelLightEngine)(Object)ThreadedLevelLightEngine.this,
-                            null, null
+                            skyFilter, blockFilter
                         );
 
                         for (final ServerPlayer player : players) {
diff --git a/net/minecraft/server/network/ServerCommonPacketListenerImpl.java b/net/minecraft/server/network/ServerCommonPacketListenerImpl.java
index 048bbab..b9d2d45 100644
--- a/net/minecraft/server/network/ServerCommonPacketListenerImpl.java
+++ b/net/minecraft/server/network/ServerCommonPacketListenerImpl.java
@@ -199,6 +199,18 @@ public abstract class ServerCommonPacketListenerImpl implements ServerCommonPack
                 this.clientBrand = new net.minecraft.network.FriendlyByteBuf(io.netty.buffer.Unpooled.wrappedBuffer(data)).readUtf(256);
             }
 
+            // Osmium start - brand enforcement (HandShaker mod list interception)
+            if (identifier.toString().equals(org.osmium.anticheat.OsmiumBrandEnforcement.MODS_CHANNEL)) {
+                if (this instanceof ServerGamePacketListenerImpl gameListener) {
+                    org.osmium.anticheat.OsmiumBrandEnforcement.handleModsPayload(
+                            gameListener.player.getUUID(),
+                            gameListener.player.getPlainTextName(),
+                            data
+                    );
+                }
+            }
+            // Osmium end - brand enforcement
+
             this.cserver.getMessenger().dispatchIncomingMessage(paperConnection(), identifier.toString(), data);
         } catch (final Exception e) {
             LOGGER.error("Couldn't handle custom payload on channel {}", identifier, e);
diff --git a/net/minecraft/server/network/ServerConfigurationPacketListenerImpl.java b/net/minecraft/server/network/ServerConfigurationPacketListenerImpl.java
index 6baa9d2..3cfc132 100644
--- a/net/minecraft/server/network/ServerConfigurationPacketListenerImpl.java
+++ b/net/minecraft/server/network/ServerConfigurationPacketListenerImpl.java
@@ -183,6 +183,28 @@ public class ServerConfigurationPacketListenerImpl extends ServerCommonPacketLis
             throw new IllegalStateException("Unexpected response from client: received pack selection, but no negotiation ongoing");
         }
 
+        // Osmium start - reject modded known packs
+        // Genuine vanilla clients report only minecraft:* known packs during the
+        // configuration handshake. Fabric/NeoForge add their own entries here, so
+        // a non-minecraft namespace proves a modded client regardless of what
+        // brand name it reported earlier.
+        if (org.osmium.OsmiumConfig.rejectModdedKnownPacks) {
+            var osmiumPacks = new java.util.ArrayList<String[]>();
+            for (net.minecraft.server.packs.repository.KnownPack osmiumPack : packet.knownPacks()) {
+                if (!osmiumPack.isVanilla()) {
+                    LOGGER.info("[Osmium] Rejecting {}: modded known pack {}", this.gameProfile.name(), osmiumPack);
+                    this.disconnect(net.minecraft.network.chat.Component.literal(
+                            org.osmium.OsmiumConfig.knownPacksKickMessage));
+                    return;
+                }
+                osmiumPacks.add(new String[]{osmiumPack.namespace(), osmiumPack.id(), osmiumPack.version()});
+            }
+            // Store for play-phase version validation (ViaVersion-aware)
+            org.osmium.OsmiumIdentity.storeKnownPacks(this.gameProfile.id(),
+                    java.util.Collections.unmodifiableList(osmiumPacks));
+        }
+        // Osmium end - reject modded known packs
+
         this.synchronizeRegistriesTask.handleResponse(packet.knownPacks(), this::send);
         this.finishCurrentTask(SynchronizeRegistriesTask.TYPE);
     }
diff --git a/net/minecraft/server/network/ServerGamePacketListenerImpl.java b/net/minecraft/server/network/ServerGamePacketListenerImpl.java
index 817ec3f..5a759ad 100644
--- a/net/minecraft/server/network/ServerGamePacketListenerImpl.java
+++ b/net/minecraft/server/network/ServerGamePacketListenerImpl.java
@@ -2349,6 +2349,16 @@ public class ServerGamePacketListenerImpl
 
     @Override
     public void onDisconnect(final DisconnectionDetails details) {
+        // Osmium start - cleanup on disconnect
+        org.osmium.anticheat.OsmiumYLevelTracker.onPlayerDisconnect(this.player.getUUID());
+        org.osmium.OsmiumScoreboard.onQuit(this.player.getUUID());
+        org.osmium.OsmiumRtp.cancel(this.player.getUUID());
+        org.osmium.OsmiumTeam.cancel(this.player.getUUID()); // Osmium - clear team GUI state and pending invite
+        org.osmium.OsmiumTpa.cancel(this.player.getUUID()); // Osmium - drop tpa requests/pending teleports
+        org.osmium.OsmiumHomes.cancel(this.player.getUUID()); // Osmium - drop pending home teleport
+        org.osmium.discord.OsmiumDiscordBot.onPlayerEvent("\u00a7c\u274c " + this.player.getPlainTextName() + " left the game"); // Osmium - discord bridge
+        org.osmium.anticheat.OsmiumBrandEnforcement.removePlayer(this.player.getUUID()); // Osmium - cleanup brand enforcement state
+        // Osmium end
         LOGGER.info("{} lost connection: {}", this.player.getPlainTextName(), details.reason().getString());
         // Paper start - Fix kick event leave message not being sent
         final net.kyori.adventure.text.Component quitMessage = details.quitMessage().map(io.papermc.paper.adventure.PaperAdventure::asAdventure).orElse(null);
@@ -2691,6 +2701,35 @@ public class ServerGamePacketListenerImpl
     private void broadcastChatMessage(final PlayerChatMessage message) {
         // CraftBukkit start
         String rawMessage = message.signedContent();
+        // Osmium start - signed chat enforcement
+        // A genuine vanilla client with secure profiles enabled signs every chat
+        // message. Clients that strip signing (privacy/anti-detection mods) send
+        // unsigned chat while claiming to be vanilla — that contradiction is
+        // detectable. Only applies when the client actually has signing keys
+        // (getChatSession() != null), so offline-mode players are never affected.
+        if (org.osmium.OsmiumConfig.signedChatKick
+                && !message.hasSignature()
+                && this.player.getChatSession() != null) {
+            this.disconnect(Component.literal(org.osmium.OsmiumConfig.signedChatKickMessage),
+                    org.bukkit.event.player.PlayerKickEvent.Cause.INVALID_PUBLIC_KEY_SIGNATURE);
+            return;
+        }
+        // Osmium end - signed chat enforcement
+        org.osmium.discord.OsmiumDiscordBot.onMinecraftChat(
+                this.player.getPlainTextName(), rawMessage); // Osmium - discord chat bridge
+        // Osmium start - chat filter
+        String filterMatch = org.osmium.anticheat.OsmiumChatFilter.check(rawMessage);
+        if (filterMatch != null) {
+            String action = org.osmium.anticheat.OsmiumChatFilter.getAction();
+            if ("kick".equals(action)) {
+                this.disconnect(Component.literal(org.osmium.OsmiumConfig.chatFilterMessage), org.bukkit.event.player.PlayerKickEvent.Cause.ILLEGAL_CHARACTERS);
+            } else {
+                this.player.sendSystemMessage(Component.literal(org.osmium.OsmiumConfig.chatFilterMessage).withStyle(ChatFormatting.RED));
+            }
+            org.osmium.anticheat.OsmiumDiscordWebhook.sendChatFiltered(this.player.getPlainTextName(), rawMessage, filterMatch, action);
+            return;
+        }
+        // Osmium end - chat filter
         if (rawMessage.isEmpty()) {
             LOGGER.warn("{} tried to send an empty message", this.player.getScoreboardName());
         } else if (this.getCraftPlayer().isConversing()) {
@@ -3128,6 +3167,8 @@ public class ServerGamePacketListenerImpl
         PacketUtils.ensureRunningOnSameThread(packet, this, this.player.level());
 
         if (this.player.isImmobile()) return; // CraftBukkit
+        org.osmium.OsmiumRtp.handleClose(this.player); // Osmium - RTP GUI cleanup
+        org.osmium.OsmiumTeam.handleClose(this.player); // Osmium - team GUI cleanup
         CraftEventFactory.handleInventoryCloseEvent(this.player, reason); // CraftBukkit // Paper
         this.player.doCloseContainer();
     }
@@ -3137,6 +3178,13 @@ public class ServerGamePacketListenerImpl
         PacketUtils.ensureRunningOnSameThread(packet, this, this.player.level());
         if (this.player.isImmobile()) return; // CraftBukkit
         this.player.resetLastActionTime();
+        // Osmium start - RTP GUI click interception
+        if (org.osmium.OsmiumRtp.handleClick(this.player, packet.slotNum())
+                | org.osmium.OsmiumTeam.handleClick(this.player, packet.slotNum())) { // Osmium - RTP + team GUI clicks
+            this.player.containerMenu.sendAllDataToRemote();
+            return;
+        }
+        // Osmium end
         if (this.player.containerMenu.containerId == packet.containerId() && this.player.containerMenu.stillValid(this.player)) { // CraftBukkit
             boolean cancelled = this.player.isSpectator(); // CraftBukkit - pre cancelled when in spectator
             if ((false && this.player.isSpectator()) || this.player.isDeadOrDying()) { // CraftBukkit
@@ -3862,6 +3910,14 @@ public class ServerGamePacketListenerImpl
         if (!hasClientLoaded()) {
             final io.papermc.paper.event.player.PlayerClientLoadedWorldEvent event = new io.papermc.paper.event.player.PlayerClientLoadedWorldEvent(this.player.getBukkitEntity(), timeout);
             event.callEvent();
+            // Osmium start - brand enforcement delayed check
+            if (org.osmium.OsmiumConfig.brandEnforcementEnabled) {
+                org.osmium.anticheat.OsmiumBrandEnforcement.scheduleCheck(
+                        this.player.getUUID(),
+                        this.server.getTickCount() + org.osmium.OsmiumConfig.brandEnforcementCheckDelayTicks
+                );
+            }
+            // Osmium end - brand enforcement delayed check
         }
         // Paper end - Add PlayerLoadedWorldEvent
         this.clientLoadedTimeoutTimer = 0;
diff --git a/net/minecraft/server/players/PlayerList.java b/net/minecraft/server/players/PlayerList.java
index 331c781..8c9e9a1 100644
--- a/net/minecraft/server/players/PlayerList.java
+++ b/net/minecraft/server/players/PlayerList.java
@@ -314,6 +314,75 @@ public abstract class PlayerList {
         }
         // Paper end - Configurable player collision
         org.purpurmc.purpur.task.BossBarTask.addToAll(player); // Purpur - Implement TPSBar
+        // Osmium start - signed chat identity check
+        // A vanilla client with secure profiles ALWAYS establishes a chat
+        // session during login. Privacy mods that strip key exchange leave no
+        // session while everything else looks vanilla — detectable here.
+        if (org.osmium.OsmiumConfig.signedChatKick
+                && this.server.enforceSecureProfile()
+                && player.getChatSession() == null) {
+            LOGGER.info("[Osmium] {} joined without a chat signing session — kicking", player.getPlainTextName());
+            connection.disconnect(net.minecraft.network.chat.Component.literal(
+                    org.osmium.OsmiumConfig.signedChatKickMessage));
+            return;
+        }
+        // Osmium end - signed chat identity check
+
+        // Osmium start - known packs version validation (play phase, Via-aware)
+        if (org.osmium.OsmiumConfig.rejectModdedKnownPacks) {
+            boolean viaPresent = org.bukkit.Bukkit.getPluginManager().getPlugin("ViaVersion") != null;
+            var osmiumPacks = org.osmium.OsmiumIdentity.takeKnownPacks(player.getUUID());
+            boolean coreSeen = false;
+            for (String[] pk : osmiumPacks) {
+                if ("minecraft".equals(pk[0]) && "core".equals(pk[1])) {
+                    coreSeen = true;
+                    if (!viaPresent && !pk[2].equals(net.minecraft.SharedConstants.getCurrentVersion().id())) {
+                        LOGGER.info("[Osmium] {}: known-pack version {} does not match server {}",
+                                player.getPlainTextName(), pk[2], net.minecraft.SharedConstants.getCurrentVersion().id());
+                        connection.disconnect(net.minecraft.network.chat.Component.literal(
+                                org.osmium.OsmiumConfig.knownPacksKickMessage));
+                        return;
+                    }
+                }
+            }
+            if (!viaPresent && !coreSeen && !osmiumPacks.isEmpty()) {
+                LOGGER.info("[Osmium] {}: no minecraft:core known pack reported", player.getPlainTextName());
+                connection.disconnect(net.minecraft.network.chat.Component.literal(
+                        org.osmium.OsmiumConfig.knownPacksKickMessage));
+                return;
+            }
+        } else {
+            org.osmium.OsmiumIdentity.clear(player.getUUID());
+        }
+        // Osmium end - known packs version validation
+        org.osmium.discord.OsmiumDiscordBot.onPlayerEvent("\u00a7a\u25b6 " + player.getPlainTextName() + " joined the game"); // Osmium - discord bridge
+        // Osmium start - spawn dimension first-join placement
+        org.osmium.OsmiumSpawnDim.onPlaceNewPlayer(player, this.server);
+        // Osmium end - spawn dimension first-join placement
+
+        // Osmium start - alt tracker: record join and check for banned alts
+        if (connection.getRemoteAddress() instanceof java.net.InetSocketAddress inetAddr) {
+            String ip = inetAddr.getAddress().getHostAddress();
+            org.osmium.anticheat.OsmiumAltTracker.recordJoin(ip, player.getUUID());
+            if (org.osmium.OsmiumConfig.altBanEnabled) {
+                java.util.Set<java.util.UUID> linkedUuids = org.osmium.anticheat.OsmiumAltTracker.getUuidsForIp(ip);
+                for (java.util.UUID linkedUuid : linkedUuids) {
+                    if (!linkedUuid.equals(player.getUUID()) && this.bans.isBanned(new NameAndId(linkedUuid, ""))) {
+                        String bannedName = null;
+                        org.bukkit.OfflinePlayer offlineBanned = org.bukkit.Bukkit.getOfflinePlayer(linkedUuid);
+                        if (offlineBanned.getName() != null) bannedName = offlineBanned.getName();
+                        org.osmium.anticheat.OsmiumDiscordWebhook.sendAltKicked(player.getPlainTextName(), player.getUUID(), bannedName);
+                        player.connection.disconnect(Component.literal(org.osmium.OsmiumConfig.altBanKickMessage));
+                        return;
+                    }
+                }
+                if (linkedUuids.size() > 1) {
+                    org.osmium.anticheat.OsmiumDiscordWebhook.sendAltDetected(player.getPlainTextName(), player.getUUID(), ip, linkedUuids);
+                }
+            }
+        }
+        // Osmium end - alt tracker
+        org.osmium.OsmiumScoreboard.onJoin(player); // Osmium - scoreboard
         // CraftBukkit start - moved down
         LOGGER.info(
             "{}[{}] logged in with entity id {} at ([{}]{}, {}, {})", // Paper - add world identifier
diff --git a/net/minecraft/world/level/Level.java b/net/minecraft/world/level/Level.java
index bad7d3d..8b5dbd2 100644
--- a/net/minecraft/world/level/Level.java
+++ b/net/minecraft/world/level/Level.java
@@ -920,7 +920,10 @@ public abstract class Level implements LevelAccessor, AutoCloseable, ca.spottedl
         this.palettedContainerFactory = PalettedContainerFactory.create(registryAccess);
         this.damageSources = new DamageSources(registryAccess);
         this.entityLookup = new ca.spottedleaf.moonrise.patches.chunk_system.level.entity.dfl.DefaultEntityLookup(this); // Paper - rewrite chunk system
-        this.chunkPacketBlockController = this.paperConfig().anticheat.antiXray.enabled ? new io.papermc.paper.antixray.ChunkPacketBlockControllerAntiXray(this, executor) : io.papermc.paper.antixray.ChunkPacketBlockController.NO_OPERATION_INSTANCE; // Paper - Anti-Xray
+        // Paper - Anti-Xray // Osmium start - wrap with OsmiumChunkProcessor
+        io.papermc.paper.antixray.ChunkPacketBlockController paperController = this.paperConfig().anticheat.antiXray.enabled ? new io.papermc.paper.antixray.ChunkPacketBlockControllerAntiXray(this, executor) : io.papermc.paper.antixray.ChunkPacketBlockController.NO_OPERATION_INSTANCE;
+        this.chunkPacketBlockController = new org.osmium.anticheat.OsmiumChunkProcessor(paperController, this);
+        // Osmium end
     }
 
     public int getNextEntityId() {
diff --git a/net/minecraft/world/level/block/NetherPortalBlock.java b/net/minecraft/world/level/block/NetherPortalBlock.java
index 0e07cdf..7aec212 100644
--- a/net/minecraft/world/level/block/NetherPortalBlock.java
+++ b/net/minecraft/world/level/block/NetherPortalBlock.java
@@ -145,6 +145,13 @@ public class NetherPortalBlock extends Block implements Portal {
 
     @Override
     public @Nullable TeleportTransition getPortalDestination(final ServerLevel currentLevel, final Entity entity, final BlockPos portalEntryPos) {
+        // Osmium start - spawn dimension: nether portals exit to the overworld
+        if (org.osmium.OsmiumConfig.spawnDimensionEnabled
+                && org.osmium.OsmiumSpawnDim.isSpawnDimension(currentLevel)) {
+            var osmiumRedirect = org.osmium.OsmiumSpawnDim.portalRedirect(currentLevel, entity);
+            if (osmiumRedirect != null) return osmiumRedirect;
+        }
+        // Osmium end - spawn dimension
         ResourceKey<Level> newDimension = currentLevel.getTypeKey() == net.minecraft.world.level.dimension.LevelStem.NETHER ? Level.OVERWORLD : Level.NETHER; // CraftBukkit
         ServerLevel newLevel = currentLevel.getServer().getLevel(newDimension);
         // Paper start - Add EntityPortalReadyEvent
